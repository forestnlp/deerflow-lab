# deerflow-lab 讲义写作规范（STYLE GUIDE）

所有版本的 README 按本规范写。目标：**读者 5 分钟读完，且记得住。**

## 结构模板（顺序固定，总字数 2500–4000 中文字）

```markdown
# vNN · <一句话主题>

> 一句话钩子（悬念、反差或生活类比，15 字内）。

## 前情提要（2–3 句）
上一版留下什么坑，本版来填。跨版承接必须具体到机制名。

## 前置技术（3–5 条 bullet）
读本版需要预先知道的每个概念，一句话说清；不假设读者记得上一版。

## 原理（300–600 字 + 一张 ASCII 图）
讲"为什么"，不讲 API。允许类比，但类比之后必须回到机制。
禁止出现"显然""众所周知"。

## 代码精读（本版核心 2–4 段，每段 ≤25 行）
每段代码后紧跟 1–3 句"这里最要紧的是……"。
真实文件位置一律用 `vNN_xxx/文件名.py` 或 本体 `路径::符号` 标注。

## 跑起来（≤3 行命令）
conda run -n deerflow_lab python vNN_xxx/main.py --fake
**必须写明预期输出长什么样**（讲义说看到 X，实跑保证看到 X）。

## Python 小课堂（1 个语言点，10 行内可运行 demo）
本版用上的语言点（反射/装饰器/上下文管理器/生成器…），demo 可直接粘贴运行。

## 与市面对比（2–4 行表格或 3 句话）
同类产品怎么做这件事，DeerFlow 为什么这么做。

## 与本体差异（诚实声明，逐条）
学习版简化了什么、为什么简化。没有差异就写"本版与本体机制一致，仅规模不同"。

## 练习（1 道，改动量 <20 行，能自证对错）

## 下一步（1 句）
下一版要捅的娄子是什么。
```

## 文风三条红线

1. **当年明月的写法，不是他的段子**：用"这件事的因果"推进叙事，允许一句
   冷幽默收段；禁止堆网络梗、禁止感叹号轰炸。
2. **小灰的图解**：每个原理节至少一张 ASCII 图/表；概念之间的关系画出来，
   不用文字罗列代替结构。
3. **严谨高于一切**：
   - 数字、文件名、函数名必须与本版代码或本体实际一致；不确定就删掉该句；
   - 本体行号一律写成"约 LNNN"或直接只给 路径::符号（行号会漂移）；
   - "与本体一致"这句话只有在机制、顺序、语义三项都对上时才允许写；
   - 所有"学习版自拟/简化"必须出现在【与本体差异】节，不许埋在正文里。

## 代码规范

- 每版自包含：文件头一行注释标注"参考本体 路径"（只注释，不读本体文件）；
- 不 import 兄弟版本目录；需要的共享件在 `参考件/`，复制进本版再用；
- `main.py` 一律 `--fake` 与真实模型同一代码路径；
- 密钥只从环境变量读（`config.yaml` 写 `$VAR` 占位）；
- 运行时产物只写本版 `data/`，启动时清空，保证可重复运行。
