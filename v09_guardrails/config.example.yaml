# v09 模型网关：在线模式读环境变量，--fake 模式不需要。
# 护栏全在中间件层，与模型无关；config 只保 v01 同款的反射装配口径。
models:
  - name: demo
    display_name: 演示模型
    use: "langchain_openai:ChatOpenAI"   # 反射目标：module:attr
    model: Qwen3.8-Flash
    base_url: $DFL_BASE_URL
    api_key: $DFL_API_KEY
    temperature: 0.2
